name: Compare Form Fields
version: 0.1.0
description: Compared any 2 form fields
icon: plug
author:
  name: Magnus Walbeck
  email: admin@walbeck.dk
homepage:
demo:
keywords: grav, plugin, etc
bugs:
readme:
license: MIT

form:
  validation: strict
  fields:
    enabled:
      type: toggle
      label: Plugin status
      highlight: 1
      default: 0
      options:
        1: Enabled
        0: Disabled
      validate:
        type: bool
    text_var:
      type: text
      label: Text Variable
      help: Text to add to the top of a page
